---
- hosts: localhost
  remote_user: sam
  vars:
    video_driver: xf86-video-ati # change this for your driver
  	ruby_version: 2.3.1
  tasks:
   - include: tasks/base.yml
   - include: tasks/gui.yml
   #- include: tasks/tex.yml
   - include: tasks/dev.yml
