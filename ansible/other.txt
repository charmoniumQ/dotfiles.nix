----------
Installation
----------
Partition correctly
Choose encoding
Choose keyboard
Choose timzeone
Add user sam and add to group wheel
visudo and allow sam access

----------
Hardware specific
----------
xbacklight helper script
visudo and allow sam to execute xbacklight script without password
echo "/home/sam/.ssh/ec2_`date +%j`" | ssh-gen-key # and add to web panel
echo "/home/sam/.ssh/github_`date +%j`" | ssh-gen-key # and add to web panel
gpg --list-keys | grep 
# git config --global user.signingkey PUBLIC_KEY_THING_GOES_HERE
